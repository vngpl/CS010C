#include <iostream>
#include "Stack.h"
#include "Queue.h"
#include "BST.h"
#include "AVL.h"

int main(/* int argv, char* argv[] */) {
    AVL<int> x;
    x.insert(5);
    x.height(5);
    // int x, y, z;

    // std::cin >> x >> y >> z;

    // if (!std::cin.good()) {
    //     std::cout << "reached" << std::endl;
    //     if (std::cin.eof()) {
    //         throw std::runtime_error("end of file reached");
    //     }
    //     else if (std::cin.bad()) {
    //         throw std::runtime_error("error reading input");
    //     }
    //     else {
    //         throw std::runtime_error("invalid input");
    //     }
    // }

    std::cout << "keep goin GOAT" << std::endl;
}